@using RARIndia.Model;
@model GeneralDepartmentListModel

<select class="form-control" id="SelectedDepartmentID">
    <option value="">-----Select Depaerment-----</option>
    @if (Model.GeneralDepartmentList.Count > 0)
    {
        foreach (GeneralDepartmentModel item in Model.GeneralDepartmentList)
        {
            if (Model.SelectedDepartmentID == item.DepartmentId)
            {
                <option value="@item.DepartmentId" selected>@item.DepartmentName</option>
            }
            else
            {
                <option value="@item.DepartmentId">@item.DepartmentName</option>
            }
        }
    }
</select>
